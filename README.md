# CST-405-Talbert-Chris
To run the compiler successfully do the following command in the terminal:
make clean
make lexer
make clean
make parser

after see the MIPScode.asm generated
run the mips code through QTspim to see the result
